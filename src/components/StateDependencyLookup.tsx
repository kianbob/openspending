'use client';

// Placeholder component — will be replaced by full implementation
export function StateDependencyLookup({ states }: { states: any[] }) {
  return (
    <div className="bg-gray-50 rounded-xl p-6 text-center text-gray-500">
      <p>State dependency lookup — loading...</p>
    </div>
  );
}
